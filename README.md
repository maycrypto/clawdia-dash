# Clawdia Dashboard

> Matrix-style дашборд для твоего [OpenClaw](https://openclaw.ai) AI-агента.

## Что это

Веб-дашборд для визуализации и управления OpenClaw-агентом. Показывает задачи, процессы, скиллы и текущую активность в реальном времени.

- **Tasks** — задачи по дням, как в Todoist. Можно менять статус кликом
- **Skills** — все подключённые модули. Клик открывает полный SKILL.md
- **Processes** — крон-задачи с расписанием
- **Overview** — общая картина: аптайм, текущая задача, счётчики

## Установка (1 команда)

SSH на VPS, где стоит OpenClaw:

```bash
curl -fsSL https://raw.githubusercontent.com/maycrypto/clawdia-dash/main/install.sh | bash
```

Скрипт автоматически:
1. Найдёт OpenClaw на сервере
2. Установит Node.js, nginx, pm2 (если нужно)
3. Поднимет API-сервер
4. Соберёт дашборд
5. Настроит nginx
6. Откроет порт 80

После установки: `http://YOUR_SERVER_IP`

## Требования

- Ubuntu 20.04+ (VPS)
- OpenClaw установлен и работает
- SSH доступ с sudo

## Управление

```bash
pm2 status                 # статус
pm2 logs clawdia-api       # логи
pm2 restart clawdia-api    # перезапуск
```

## Удаление

```bash
pm2 delete clawdia-api
sudo rm /etc/nginx/sites-enabled/clawdia /etc/nginx/sites-available/clawdia
sudo systemctl restart nginx
rm -rf ~/clawdia-api ~/clawdia-dashboard
```

## Файлы

| Файл | Описание |
|------|----------|
| `install.sh` | Установочный скрипт |
| `App.jsx` | Фронтенд дашборда (React) |
| `server.js` | API-сервер (Express.js) |

## License

MIT

---

Made with 💚 for the OpenClaw community
