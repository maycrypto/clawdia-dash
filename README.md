# Clawdia Dashboard

> Matrix-style дашборд для твоего [OpenClaw](https://openclaw.ai) AI-агента.

## Что это

Веб-дашборд для мониторинга и управления OpenClaw-агентом в реальном времени.

- **Tasks** — задачи по дням, с фильтрами. Можно менять статус кликом
- **Skills** — все модули (системные + кастомные). Клик открывает полный SKILL.md
- **Processes** — крон-задачи с расписанием и временем запуска
- **Overview** — аптайм, текущая задача, версия, счётчики

## Установка

### Шаг 1: Установи дашборд (1 команда)

SSH на VPS, где стоит OpenClaw:

```bash
curl -fsSL https://raw.githubusercontent.com/maycrypto/clawdia-dash/main/install.sh | bash
```

Скрипт автоматически установит фронтенд, nginx и всё настроит.

### Шаг 2: Подключи API (скинь агенту)

После установки скинь своему OpenClaw-агенту спецификацию API:

```
Прочитай файл ~/clawdia-api/API-SPEC.md и создай API-сервер по этой спецификации. Найди где на сервере хранятся задачи, скиллы, процессы и подключи их. Запусти через pm2.
```

Или скопируй [API-SPEC.md](./API-SPEC.md) и отправь агенту.

Агент сама найдёт структуру данных и поднимет API под свою конфигурацию.

### Шаг 3: Открой дашборд

```
http://YOUR_SERVER_IP
```

## Почему агент сама настраивает API?

У каждого пользователя OpenClaw может хранить данные в разных местах. Агент знает свою файловую систему лучше любого универсального скрипта, поэтому она сама реализует API-сервер и подключает реальные источники данных.

## Обновление

```bash
curl -fsSL https://raw.githubusercontent.com/maycrypto/clawdia-dash/main/install.sh | bash
```

Та же команда — обновит фронтенд. API трогать не будет.

## Удаление

```bash
pm2 delete clawdia-api
sudo rm -f /etc/nginx/sites-enabled/clawdia /etc/nginx/sites-available/clawdia
sudo systemctl restart nginx
rm -rf ~/clawdia-api ~/clawdia-dashboard
```

## Файлы

| Файл | Описание |
|------|----------|
| `install.sh` | Установка фронтенда + nginx |
| `App.jsx` | Фронтенд дашборда (React) |
| `API-SPEC.md` | Спецификация API для агента |

## License

MIT

---

Made with 💚 for the OpenClaw community
